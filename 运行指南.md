# SMB文件浏览器 - 运行指南

## 方法一：使用Android Studio（推荐）

### 步骤1：安装Android Studio
1. 访问 https://developer.android.com/studio
2. 下载并安装Android Studio
3. 启动Android Studio，完成初始配置

### 步骤2：创建新项目
1. 打开Android Studio
2. 点击 "New Project"
3. 选择 "Empty Views Activity"
4. 填写项目信息：
   - Name: SMBFileBrowser
   - Package name: com.smbfilebrowser
   - Language: Java
   - Minimum SDK: API 24

### 步骤3：复制代码文件
把以下文件内容复制到对应位置：

#### 1. SMBManager.java
路径：`app/src/main/java/com/smbfilebrowser/SMBManager.java`
```java
[文件内容见下方]
```

#### 2. MainActivity.java
路径：`app/src/main/java/com/smbfilebrowser/MainActivity.java`
```java
[文件内容见下方]
```

#### 3. FileBrowserActivity.java
路径：`app/src/main/java/com/smbfilebrowser/FileBrowserActivity.java`
```
[文件内容见下方]
```

### 步骤4：添加依赖
打开 `app/build.gradle`，在 dependencies 中添加：
```gradle
dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.11.0'
    implementation 'androidx.recyclerview:recyclerview:1.3.2'
    implementation 'androidx.swiperefreshlayout:swiperefreshlayout:1.1.0'
    
    // SMB库
    implementation 'eu.agno3.jcifs:jcifs-ng:2.1.10'
    
    // 视频播放
    implementation 'androidx.media3:media3-exoplayer:1.2.1'
    implementation 'androidx.media3:media3-ui:1.2.1'
}
```

### 步骤5：添加权限
打开 `app/src/main/AndroidManifest.xml`，添加：
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
```

### 步骤6：编译运行
1. 连接手机或启动模拟器
2. 点击工具栏的绿色运行按钮（▶️）
3. 等待编译完成，应用会自动安装到手机

---

## 方法二：命令行编译

### 前提条件
- 安装JDK 11或更高版本
- 安装Android SDK
- 配置环境变量

### 步骤
```bash
# 1. 进入项目目录
cd smb-file-browser

# 2. 编译APK
./gradlew assembleDebug

# 3. 安装到手机
adb install app/build/outputs/apk/debug/app-debug.apk
```

---

## 使用方法

1. **打开应用**
2. **输入服务器地址**：Windows电脑的IP（如 192.168.1.100）
3. **输入共享名称**：文件夹共享名（如 SharedFolder）
4. **输入用户名密码**（如果需要）
5. **点击连接**
6. **浏览文件**：点击文件夹进入，点击文件打开

---

## 常见问题

### 连接失败
- 检查手机和电脑是否在同一WiFi
- 检查Windows防火墙是否允许SMB
- 检查IP地址和共享名称是否正确

### 无法播放视频
- 确保视频格式受支持（MP4/MKV/AVI）
- 检查网络连接是否稳定
